import React, { FC, Component } from 'react';
// import Customers from '../../core/Customers/Customers';
// import {
//   checkPageIsValid
// } from '../../../store/selectors/Accounts';
const CustomersPage: React.FC = props => {
  // var isValid = checkPageIsValid();
  // if (!isValid) {
  //   window.location.href = "/home";
  // }
    return (
      <div>
        {/* {isValid === true && (
          <Customers />
        )} */}
        </div>
    );
};

export default CustomersPage; 
